{
  "lexicon": 1,
  "id": "site.standard.heron.getDocuments",
  "defs": {
    "main": {
      "type": "query",
      "description": "List documents with full hydration. Optionally filter by publication or author.",
      "parameters": {
        "type": "params",
        "properties": {
          "publication": {
            "type": "string",
            "format": "at-uri",
            "description": "AT URI of a publication to filter by."
          },
          "author": {
            "type": "string",
            "format": "did",
            "description": "DID of the document author to filter by."
          },
          "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 100,
            "default": 50
          },
          "cursor": {
            "type": "string"
          }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["documents"],
          "properties": {
            "cursor": {
              "type": "string"
            },
            "documents": {
              "type": "array",
              "items": {
                "type": "ref",
                "ref": "site.standard.heron.defs#documentView"
              }
            }
          }
        }
      }
    }
  }
}
