{
  "lexicon": 1,
  "id": "app.rocksky.actor.getActorScrobbles",
  "defs": {
    "main": {
      "type": "query",
      "description": "List an actor's scrobbles (listening history), ordered by most recent.",
      "parameters": {
        "type": "params",
        "required": ["did"],
        "properties": {
          "did": { "type": "string", "format": "did" },
          "limit": { "type": "integer", "minimum": 1, "maximum": 100, "default": 50 },
          "offset": { "type": "integer", "minimum": 0, "default": 0 }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "object",
          "required": ["scrobbles"],
          "properties": {
            "scrobbles": {
              "type": "array",
              "items": {
                "type": "ref",
                "ref": "app.rocksky.actor.defs#scrobbleView"
              }
            }
          }
        }
      }
    }
  }
}
