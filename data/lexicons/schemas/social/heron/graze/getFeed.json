{
  "lexicon": 1,
  "id": "social.heron.graze.getFeed",
  "defs": {
    "main": {
      "type": "query",
      "description": "Get a Graze feed generator for the authenticated account. The AppView proxies to the Graze API with its own platform credentials; the caller's service-auth only establishes the viewer DID.",
      "parameters": {
        "type": "params",
        "required": ["rkey"],
        "properties": {
          "rkey": {
            "type": "string",
            "description": "Record key of the feed generator (app.bsky.feed.generator)."
          }
        }
      },
      "output": {
        "encoding": "application/json",
        "schema": {
          "type": "ref",
          "ref": "social.heron.graze.defs#feedResult"
        }
      }
    }
  }
}
